databaseChangeLog:
  - changeSet:
      id: 6
      author: Denys Funderat
      changes:
        - sql:
            sql: "INSERT INTO users_roles (user_id, role_id)
                  SELECT u.id, (SELECT r.id FROM roles r WHERE r.name = 'USER')
                  FROM users u"